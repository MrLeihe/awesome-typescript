function identity(value, message) {
    return value;
}
identity('stone', 1);
class LanguageDetail {
}
let myLanguage = new LanguageDetail();
myLanguage.name = 1;
myLanguage.add = (x, y) => {
    return x + y;
};
let obj = { a: 1, b: 2, c: 3 };
function getLength(arg) {
    return arg.length;
}
getLength({ length: 10 });
let mp = {};
